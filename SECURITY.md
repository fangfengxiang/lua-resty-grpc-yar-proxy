# 安全策略

## 支持的版本

本项目目前仅对以下版本提供安全更新：

| 版本 | 支持状态 |
|------|----------|
| 0.1.x | :white_check_mark: |
| < 0.1.0 | :x: |

## 报告漏洞

如果你发现安全漏洞，请**不要**在公开 Issue 中提交。

请通过以下方式私下报告：

1. 发送邮件至 [维护者邮箱](mailto:fangfengxiang836@gmail.com)
2. 在邮件中包含：
   - 漏洞描述和影响范围
   - 复现步骤
   - 可能的修复建议（如有）

我们将在收到报告后 **48 小时内**确认收到，并在 **7 天内**评估严重程度和影响范围，
随后发布修复版本或提供临时缓解方案。

## 安全注意事项

本库作为 gRPC ↔ YAR 协议代理运行在 OpenResty 中，使用时请注意：

- **YAR Server URL**：`services` 配置中的 YAR Server 地址应为可信内网地址，避免暴露至公网
- **gRPC 客户端认证**：如需对 gRPC 客户端进行身份验证，请在 nginx 层配置 TLS 客户端证书
  或 mTLS（本库不处理认证逻辑）
- **请求体大小**：建议在 nginx 配置中设置 `client_body_buffer_size` 和 `client_max_body_size`
  以防止超大请求体导致内存问题
- **超时配置**：务必为每个服务配置合理的 `timeout` 和 `connect_timeout`，防止后端 YAR Server
  不可用时连接堆积
